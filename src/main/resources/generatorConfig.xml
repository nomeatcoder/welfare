<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
        PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <context id="MysqlContext" targetRuntime="MyBatis3Simple" defaultModelType="flat">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>

        <plugin type="tk.mybatis.mapper.generator.MapperPlugin">
            <property name="mappers" value="cn.nomeatcoder.utils.MyMapper"/>
        </plugin>

        <jdbcConnection driverClass="com.mysql.jdbc.Driver"
                        connectionURL="jdbc:mysql://192.168.0.10:3306/welfare?characterEncoding=utf8"
                        userId="root"
                        password="root">
        </jdbcConnection>

        <!-- 对于生成的pojo所在包 -->
        <javaModelGenerator targetPackage="cn.nomeatcoder.domain" targetProject="src/main/java"/>

        <!-- 对于生成的mapper所在目录 -->
        <sqlMapGenerator targetPackage="mapper" targetProject="src/main/resources"/>

        <!-- 配置mapper对应的java映射 -->
        <javaClientGenerator targetPackage="cn.nomeatcoder.mapper" targetProject="src/main/java"
                             type="XMLMAPPER"/>

        <!-- 表名 -->
        <table tableName="cart"></table>
        <table tableName="category"></table>
        <table tableName="order"></table>
        <table tableName="order_item"></table>
        <table tableName="pay_info"></table>
        <table tableName="product"></table>
        <table tableName="shipping"></table>
        <table tableName="user"></table>

    </context>
</generatorConfiguration>